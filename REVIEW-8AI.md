# 8-AI Review Results — AI-DLC Workflows 2.0 整理ノート

実施日: 2026-07-28  
対象: 本リポジトリのドキュメント一式（ドキュメント精度レビュー）  
種別: feature ブランチ向けコードレビュー手順を、**二次整理ドキュメントの事実性レビュー**に転用

---

## サマリー表

| AI | Status | HIGH | MEDIUM | LOW | 判定 |
|----|--------|------|--------|-----|------|
| Session self | ✅ Complete | 0 | 3 | 4 | APPROVE WITH NITS |
| Codex CLI | ✅ Complete（再実行後） | 0 | 3 | 1 | NEEDS FIXES |
| Copilot CLI | ✅ Complete | 0 | 1 | 3 | APPROVE WITH NITS |
| Antigravity (agy) | ✅ Complete | 3* | 1 | 1 | NEEDS FIXES* |
| Grok CLI | ✅ Complete | 0 | 6 | 8 | APPROVE WITH NITS |
| Cursor Agent | ✅ Complete（復旧後） | 0 | 7 | 6 | APPROVE WITH NITS |
| Devin CLI | ✅ Complete | 0 | 5 | 1 | NEEDS FIXES |
| Kiro CLI | ✅ Complete（復旧後） | 1* | 8 | 4 | APPROVE WITH NITS |

\* 下記「HIGH の裁定」参照。一次ソース照合の結果、AI-DLC 事実誤認としての **ブロッキング HIGH は 0**。

---

## レビュー対象

- ドキュメントセット: README + 01〜09 + SOURCES（レビュー時点）
- 対照一次情報: `awslabs/aidlc-workflows` v2（`AIDLC_VERSION=2.5.11`）の README / docs / CHANGELOG / core
- 送信前: クレデンシャル類のスキャン済み

---

## HIGH の裁定（オーケストレータ）

| 指摘元 | 主張 | 裁定 |
|--------|------|------|
| Antigravity | GA が invented（Reddit preview のみ） | **却下**。v2 README に *Announcing 2.0 (GA)* / *generally available* |
| Antigravity | Opus 4.8 / Codex≥0.145 / opencode≥1.17 が invented | **却下**。公式 README 記載 |
| Antigravity | builder.aws.com が internal で privacy 違反 | **却下（過剰）**。公開の AWS Builder コミュニティ URL |
| Kiro | 整理者モデル記録の厳密性 | **運用注意**（AI-DLC 事実誤りではない） |

**結論: 公開を阻む HIGH（AI-DLC の誤誘導）は 0 件。**

---

## 合意の高い MEDIUM と対応状況

| ID | 内容 | 状態 |
|----|------|------|
| M1 | ゲート例外（autonomous / Init / Verification） | ✅ 反映済 |
| M2 | Bedrock 出荷既定 vs ハーネス別必須 | ✅ 反映済 |
| M3 | Construction 3.1–3.4 CONDITIONAL | ✅ 反映済 |
| M4 | hub-and-spoke ≠ mode 名（`mode: subagent`） | ✅ 反映済 |
| M5 | tools 本数の出典差（25 vs 約30） | ✅ 反映済 |
| M6 | mvp スキップ詳細（Ideation + Operation） | ✅ 反映済 |
| M7 | CodeKB は Kiro で workspace-scan 固定 | ✅ 反映済（**2.5.74 で状況が変わった**。Kiro CLI は接続可能になり、固定なのは Kiro IDE のみ → [04-agents.md](./04-agents.md) 4.4） |
| M8 | Codex trust 手順の補足 | ✅ 反映済 |
| M9 | 1.x 対比の留保強化 | ✅ 反映済 |
| M10 | Spec PDF は `assets/`、正本優先順位 | ✅ 反映済 |

LOW は対応任意（未対応）。

---

## 正確と確認された中核

> **本節は 2026-07-28・実装 2.5.11 時点の確認結果**であり、現在値ではない。
> **2.6.2（CHANGELOG 日付 2026-08-13 / 取得日 2026-08-14）時点では 5 フェーズ / 33 ステージ / 14 agents / 9 scopes / 7 harnesses、
> センサー 6 種、監査 82 イベント種別（21 分類）、mode 集計 29 inline / 2 subagent / 1 pipeline / 1 mob。**
> 変化の経緯は [11-release-impact-2562.md](./11-release-impact-2562.md) と
> [12-release-impact-2602.md](./12-release-impact-2602.md) を参照。
>
> **時点限定（追記 2026-08-22）**: 上記も 2.6.2 時点の記録。**上流実測（HEAD `71d9a9e0` / 実装 2.6.49）**
> ではスコープが **11**、監査イベントが **86 種（22 分類）**に変わっている。5 フェーズ / 33 ステージ /
> 14 agents / センサー 6 / TypeScript フック 17 / 7 harnesses は不変。→ [13-release-impact-2649.md](./13-release-impact-2649.md)
>
> **さらに追記（2026-08-22・同日の再同期）**: 上流は **2.6.55**（HEAD `840ba653`）まで進んだが、
> **中核メトリクスは全項目不変**で、`stage-graph.json` / `scope-grid.json` は 2.6.49 と
> **バイト単位で一致**する。変わったのは実行時のガードと継続トークンと監査の発火条件である。
> → [14-release-impact-2655.md](./14-release-impact-2655.md)
>
> **さらに追記（2026-08-29）**: 上流は **2.6.123**（HEAD `2fbee12f`）まで進み、今度は中核メトリクスが動いた。
> **TypeScript フック 17 → 18 / `core/tools/*.ts` 41 → 51 / 監査 86 → 91 イベント種別**、
> スコープのステージ数が **`bugfix` 7/33 → 9/33・`refactor` 8/33 → 10/33**（2.6.70）。
> ステージ 33 / スコープ数 11 / 14 agents / センサー数 6 / 監査カテゴリ 22 / 7 harnesses は不変。
> **51 は CLI の本数ではない**（起動可能な CLI は 32、残り 19 はライブラリモジュール）。
> → [15-release-impact-26123.md](./15-release-impact-26123.md)
>
> **さらに追記（2026-09-01）**: 上流は **2.7.0**（`main` HEAD `96b11d39`）まで進んだが、
> **中核メトリクスは全項目不変**である。今回変わったのはブランチ構成で、
> **`v2` ブランチが削除され `main` が 2.x の正本になった**（旧 1.x は新設 `v1` ブランチへ）。
> **ロジック**が動いたのは 2.6.124 の 1 版のみで、**2.7.0 が変えたのはバージョン定数だけ**である。
> → [16-release-impact-2700.md](./16-release-impact-2700.md)

**⚠ 以下の箇条書きは 2.5.11 時点の確認結果であり、現行値ではない。**
現行値は上の追記ブロック（2026-09-01 / 2.7.0）を参照すること。
**ステージ 32 → 33、scopes 9 → 11、harnesses 5 → 7、センサー 5 → 6、監査 74 → 91 と、
すべて動いている。**

- 5 フェーズ / 32 ステージ / 14 agents / 9 scopes / 5 harnesses
- バージョン 2.5.11、GA 表記、MIT-0（上流実装）
- Engine / Conductor、Bolt / walking skeleton / ladder
- 学習ループは「次回ワークフローから」
- センサー 5 種、監査 74 イベント種別
- mode 集計 28 inline / 2 subagent / 1 pipeline / 1 mob

---

## 総合判定（ドキュメント精度レビュー時点）

| 観点 | 判定 |
|------|------|
| 事実誤認（HIGH, 裁定後） | **0** |
| MEDIUM | **対応済み** |
| ドキュメント精度 | **APPROVE WITH NITS**（LOW のみ残） |

---

## 公開可否レビュー（2026-07-28）

観点: privacy / 二次整理の明示 / ライセンス / 公開体裁

| AI | Status | privacy | 判定 |
|----|--------|---------|------|
| Session self | ✅ | clean | PUBLISH WITH NITS |
| Codex CLI | ✅ | clean | PUBLISH WITH NITS |
| Copilot CLI | ✅ | clean | PUBLISH WITH NITS |
| Antigravity (agy) | ✅ | clean | PUBLISH |
| Grok CLI | ✅ | clean | PUBLISH WITH NITS |
| Cursor Agent | ✅ | clean | PUBLISH WITH NITS |
| Devin CLI | ✅ 単独リトライ後 | clean | PUBLISH WITH NITS |
| Kiro CLI | ✅ | clean | MEDIUM は免責強化等 |

### 公開レビューで合意した MEDIUM と対応

| 指摘 | 対応 |
|------|------|
| 冒頭の非公式・二次整理バナー強化 | ✅ README 冒頭に免責ブロック |
| 上流 MIT-0 と本リポ MIT の混同 | ✅ メトリクス表を分離 |
| 作業ログを公開資料と並べすぎ | ✅ README で「公開向け / メンテナ向け」分割 |
| 09 見出し「ローカルパス」 | ✅ 「上流リポジトリ内パス」へ変更 |

### Devin CLI — 単独リトライ結果（2026-07-28）

**経緯**: 公開可否レビューの **7 並列実行時**は stdout 空で helper が `Devin CLI returned no output.` を返した。CLI 未導入ではなく、並列時の空レスポンスと判断。

**再診断**:
- `devin` 3000.2.17、認証 OK
- 極小 smoke 成功
- 同一公開プロンプトを **単独実行** → 成功（下記）

**Devin の指摘一覧（単独リトライ）**:

| # | Severity | File | Issue | 本リポジトリでの扱い |
|---|----------|------|-------|----------------------|
| 1 | MEDIUM | `09-references.md`, `SOURCES.md` | Method Definition Paper の URL が Amplify 既定ドメイン（`prod.d13rzhkk8cj2z0.amplifyapp.com`）で、公式ホストか読者に分かりにくい | **既知**。上流 README が同 URL を参照している。二次整理として踏襲。公式ドメインが確認でき次第差し替え（任意） |
| 2 | LOW | `README.md` | メトリクス表のライセンスが MIT-0 で本リポ MIT と混同しうる | **対応済み**（上流 MIT-0 / 本ノート MIT を分離） |
| 3 | LOW | 複数 | 「端到端」→「エンドツーエンド」 | 任意（`docs/REMAINING_TASKS.md` に記載） |
| 4 | LOW | `06-harnesses-install.md` | Kiro「≥2.6」が CLI 版か曖昧 | 任意（Kiro **CLI** バージョンの意味で記載） |

Devin 判定: **PUBLISH WITH NITS** / **privacy: clean** / HIGH なし

### 公開可否（オーケストレータ）

- **HIGH ブロッカー: なし**（privacy: clean が **8 系統すべて**で一致）
- **Devin: 単独リトライで完了**（並列時のみ失敗）
- **判定: PUBLISH WITH NITS**（公開向け nit は主要対応済み。Devin #1 の Amplify URL は上流踏襲の任意改善）
- **リモート push はユーザー最終確認後のみ**

---

## 8AI レビュー（2026-09-01 / 2.6.123 → 2.7.0・ブランチ再編）

対象: 新章 [16-release-impact-2700.md](./16-release-impact-2700.md) と、既存 15 ファイルの改訂差分。
非公開リポジトリ側の同区間レポートも同一プロンプトで併せてレビューした
（**送信前に機密識別子をマスクし、残存 0 件を機械確認**）。

| AI | Status | HIGH | MEDIUM | LOW | 判定 |
|----|--------|------|--------|-----|------|
| Session self | ✅ Complete | 0 | 3 | 2 | NEEDS FIXES（自己検出） |
| Codex CLI | ✅ Complete | 1 | 4 | 1 | NEEDS FIXES |
| Cursor Agent | ✅ Complete | 2 | 3 | 2 | NEEDS FIXES |
| Copilot CLI | ✅ Complete | 2 | 3 | 1 | NEEDS FIXES |
| Antigravity (agy) | ✅ Complete | 0 | 2 | 2 | APPROVE WITH NITS |
| Devin CLI | ✅ Complete（再実行後） | 1 | 0 | 1 | NEEDS FIXES |
| Kiro CLI | ⚠ 未実施 | — | — | — | 本環境の `kiro` は IDE の CLI で、`kiro chat` が stdout に出力しない（極小プロンプトでも空） |
| Grok CLI | ⚠ 除外 | — | — | — | ユーザー指示により今回除外 |

> **Devin の経緯**: 初回は `Refusing to run in an untrusted workspace`、次に非対話モードで
> ツール確認により停止した。プロンプト冒頭に「ツールを使わずテキストのみで回答」と明記して
> リポジトリ内から再実行したところ完了した。`--permission-mode dangerous` は付与していない。

### 採用した指摘

| # | 指摘元 | Severity | 内容 | 対応 |
|---|--------|:--:|---|---|
| 1 | Codex / Cursor | **HIGH** | 「2.6.1 より前のワークフロー拒否」を **2.7.0 が追加した前提**として書いていた。2.7.0 はバージョン定数以外を変えていないので両立しない | ✅ **2.6.1 の永続 state スキーマ v8 の再掲**と書き直した（16.4 / 6.4） |
| 2 | Copilot / Cursor / Devin / self | **HIGH** | 非公開側レポートの変更内訳表が合計 76 で、総数 74 と合わない | ✅ 再集計（`docs` 22→18、`scripts` 2 を追加）。4+28+18+15+5+2+2=74 |
| 3 | self | **HIGH** | 16.1 の表で「v2 系のタグ無し」と書いていたが、`v2.1.1` / `v2.2.0` / `v2.3.0` は以前から存在する | ✅ 「GitHub Release の Latest が `v1.0.1` → `v2.7.0`」に訂正。**2.x が初めてリリース公開された**ことを新知見として追記 |
| 4 | self | MEDIUM | roadmap から外れた 7 件を「いずれも出荷済みへ移った」と書いたが、**#754 は移動先が無い** | ✅ 6 件と #754 を分け、#754 は「移動先が無い。取り下げか記載漏れか判断できない」と明記 |
| 5 | self | MEDIUM | `.github/ISSUE_TEMPLATE/` を「1.x の main から引き継がれた」と書いたが、**`v1` に存在しない** | ✅ `labeler.yml` のみ引き継ぎ、テンプレート類はこの移行で新設と訂正 |
| 6 | Cursor | MEDIUM | 「履歴の書き換えなし」が `main` の force-update と衝突しうる | ✅ 「旧 v2 先端→現 main は線形。ただし `main` 自体は force-update。旧 main の clone は `reset --hard` か再 clone」と書き分け |
| 7 | Codex / Cursor | MEDIUM | 「上げる作業は dist 再コピーだけ」が同文書内の注意と矛盾 | ✅ 「配布物の差替えは再コピーで足りる。ただし取得元が `main` に変わり、既存 state と履歴は残る」と分けた |
| 8 | Codex | MEDIUM | 非公開側で「数値も機序も 1 つも動いていない」が過大な一般化 | ✅ 「本レポート群が依拠する数値と機序」に射程を限定 |
| 9 | Antigravity / Cursor | MEDIUM | 非公開側の追記ブロックと差分表の時系列が逆転（2.6.55 → 2.7.0 → 2.6.123） | ✅ 4 ファイルで昇順に並べ直した |
| 10 | Antigravity / Codex / Cursor / self | LOW | 16.8 の本文が「10 章から 15 章の `基準:` 行」なのに表は 11〜15 章 | ✅ 10 章は `対象:` 行で HEAD SHA を持たない旨を明記。SOURCES / TRACEABILITY の同一記述も同期 |
| 11 | Cursor | LOW | 16.2 の不変メトリクス表と CONVERSATION_LOG の列挙に **センサー 6** が抜けている | ✅ 追加（両 ref で `core/sensors/` はバイト単位で同一を確認） |
| 12 | Antigravity | LOW | 非公開側の比較レポート冒頭の「差分の全体像」参照先が前区間のまま | ✅ 最新区間へ更新 |
| 13 | Codex | LOW | 追従実績表に「3 日で 2 件」の行を足したのに「ペースは落ちていない」評価が残る | ✅ 評価の射程が 3 期目までである旨と、**追従コストが件数に比例しない**ことを注記 |
| 14 | Devin | LOW | 追従実績表の「約 3 日」と 16 章の「期間 4 日」が食い違って見える | ✅ 数え方が違うだけ（取得日の経過日数 / 上流コミット日の両端含み）。既存表の慣例に合わせ「約 3 日（上流は 4 日分）」とし、16 章に「両端を含む」を明記 |
| 15 | self | LOW | 「2.6.1 〜 2.6.124 の 124 版分」は誤り（欠番がある）／「2.6.1 は 2026-08-14 頃」 | ✅ 「2.6.x サイクル全体（公開 CHANGELOG 上 95 エントリ）」／CHANGELOG 日付 **2026-08-13** に訂正 |

### GitHub 側 AI レビューで採用した指摘（2 巡目）

| # | 指摘元 | Severity | 内容 | 対応 |
|---|--------|:--:|---|---|
| G1 | Codex bot (P2) / Devin bot | **重要** | 「`v2.7.0` で初めて版を pin できる」は誤り。`v2.1.1` / `v2.2.0` / `v2.3.0` のタグは以前から存在し、GitHub Release の有無と pin の可否は別 | ✅ 「Releases 一覧と Latest 表示が変わった発見性の改善」に訂正。両リポジトリ同期 |
| G2 | Copilot（9 箇所で指摘） | **重要** | 「2.7.0 のコード変更はゼロ」は、同じ文書が `AIDLC_VERSION` 更新に触れている以上、事実として矛盾 | ✅ 全 11 ファイルを「変えたのはバージョン定数だけ／ロジックの変更なし」に機械置換 |
| G3 | Devin bot 🟡（非公開側） | **重要** | 「再コピーで足りる」が **`/aidlc plugin sync` の必須手順を落としている**。2.7.0 の Upgrade 文が明示している | ✅ 公開 16.4・6.4、非公開 §6 に追加。エンジン入れ替えでプラグイン合成が失われる旨も明記 |
| G4 | Codex bot (P1、非公開側) | **重要** | 置換後の clone を `--branch main` と案内していたが、**`main` は動くブランチ**。版固定が採用条件なのに評価外の版が入りうる | ✅ 社内向けは `--branch v2.7.0`（または SHA）で固定するよう明記 |
| G5 | Codex bot (P2、非公開側) | 中 | 「`v2` 指定は fetch に失敗する」は不正確。**ローカル `v2` ブランチが残る使い回し clone では `git checkout v2` が今も成功し、静かに旧コミットのまま走る** | ✅ 「止まる」「静かに古いまま動く」の 2 通りに分けた。検出は `git ls-remote --heads origin v2` |
| G6 | Devin bot 🔴 | 中 | `git reset --hard origin/main` を既定手順として案内しており、未コミット変更とローカル履歴が失われる | ✅ 既定を「別ディレクトリへ clone し直す」に変更。同一 checkout を使う場合は退避を先にと明記 |
| G7 | Copilot | 中 | `REVIEW-8AI.md` の AI 一覧表が途中の blockquote で分断され、Markdown の表として壊れている | ✅ 注記を表の外へ移動 |
| G8 | Copilot | 中 | `--depth 1` の shallow clone では `git diff 2fbee12f..origin/main` が再現できない | ✅ SOURCES と 16.3 に `--unshallow` が要る旨を注記 |
| G9 | Copilot | 低 | `SOURCES.md` の clone コマンドにリポジトリ URL が無く実行不能 | ✅ 完全形に |
| G10 | Devin bot 🔍（非公開側） | 低 | ブランチ構成・Latest・進行中 PR はリモートの現在状態で、引用時に再確認が要る | ✅ §8 に確認日つきで明記 |

### GitHub 側 AI レビューで採用した指摘（3 巡目）

**3 巡目の指摘は、ほぼ「2 巡目の修正が全コピーに波及していなかった」ことの検出だった。**
同じ事実の別コピーを取りこぼす癖が、また同じ形で出た。

| # | 指摘元 | Severity | 内容 | 対応 |
|---|--------|:--:|---|---|
| H1 | Devin bot 🔴 | **重要** | `git reset --hard origin/main` の警告を**公開側にしか入れていなかった**。非公開側 §2 は既定手順のまま | ✅ 非公開側も同文に同期 |
| H2 | Devin bot 🟡 / Codex bot (P2) | **重要** | 「`v2` 指定は fetch に失敗する」の訂正を **`docs/REMAINING_TASKS.md` にしか入れていなかった**。非公開側 §2 は断定のまま | ✅ 非公開側 §2 も 2 通りに分けた |
| H3 | Codex bot (P1) / Copilot | **重要** | 版固定の訂正が**レポート本体にしか入っておらず**、A 節（ハーネス開発チームへの申し送り）は `--branch main` のまま。**申し送りは runbook に写される前提の文書**なので、ここが一番危ない | ✅ 申し送りに `--branch v2.7.0`（または SHA）を明記 |
| H4 | Codex bot (P2) | 中 | 16.4 に「2.7.0 にコード変更は無い」が 1 箇所残っていた（機械置換の取りこぼし） | ✅ 訂正 |
| H5 | Codex bot (P2) / Devin bot 🔍 | 中 | Spec PDF の「両方の ref に存在する」が、`v2` 削除後の表現として不正確 | ✅ 実測して書き直し（下記） |
| H6 | Copilot | 低 | 追加した改訂日行が太字のネスト（`**…**…**`）で強調が壊れている | ✅ 内側の強調を外した（過去 4 行の同型は当時の記録として据え置き。別途の整理課題） |

**H5 の実測**: Codex は「`blob/v2/...` はもう解決しない」と述べたが、**これは誤りだった**。
実際に叩くと **`blob/v2/...` も `tree/v2` も 302 で `main` に転送される**（2026-09-01 実測）。
Devin の観察のほうが正確である。**Git 操作は失敗するのに Web リンクは生きている**という食い違いは
読者が誤判断しやすいので、両章に実測値つきで明記した。

### GitHub 側 AI レビューで採用した指摘（4 巡目）

| # | 指摘元 | Severity | 内容 | 対応 |
|---|--------|:--:|---|---|
| I1 | Codex bot (P2) | **重要** | 非公開側 §6 の「止まる 4 版」表で **2.6.94 を「同上」に、2.6.78 を「CI が止まる」に潰していた**。実際の契機は版ごとに違い、これでは調査対象を取り違える | ✅ 15 章 §7 の原文に戻した（下記） |
| I2 | Codex bot (P2) / Copilot / Devin bot 🔍 | **重要** | 旧 Web URL の挙動が同一文書内で矛盾（A 節「302 転送」／未確認事項「404」／PR 本文「404」） | ✅ 実測どおり 302 に統一。PR 本文も訂正 |
| I3 | Copilot | 中 | 公開側の quickstart・6.7 の確認コマンドが**動くブランチ `main`** を clone しており、本ノートが記述している版と食い違いうる | ✅ 6.7 はタグ `v2.7.0` 固定を既定に。README・9 章には固定方法を併記 |

**I1 の内訳**（`analysis/IMPACT-2655-to-26123.md` §7 が正）:

| 版 | いつ止まるか |
|---|---|
| 2.6.121 | compose / graph compile を走らせたとき |
| 2.6.65 | graph compile を走らせたとき（`produces` の重複宣言） |
| **2.6.94** | **改名された内部ヘルパを import しているカスタムツールを起動したとき** |
| **2.6.78** | **CI から `plugin sync` を呼んでいて、設定済みプラグインルートに使える compose フックが 0 本のとき** |

契機が違えば確認対象も違う。「プラグインを使っているか」だけでは切り分けられない。
**要約のつもりで「同上」と書いたことが、そのまま調査の抜けにつながる書き方だった。**

### GitHub 側 AI レビュー（5 巡目）

| # | 指摘元 | Severity | 内容 | 対応 |
|---|--------|:--:|---|---|
| J1 | Codex bot (P2) | 中 | 9.4 に付けたコメント（「固定するなら `v2.7.0`」）と、直後の実コマンド（`--branch main`）が食い違っている | ✅ **ただし Codex の提案どおりには直さなかった**（下記） |

**J1 の裁定**: 矛盾の指摘は正しいが、**提案された「9.4 もタグに固定する」は誤りである。**
9.4 は「本ノートの更新のしかた」——**上流がどこまで進んだかを知るための節**なので、
タグに固定すると差分の検知そのものができなくなる。`main` を見るのが正しい。
問題は私が用途の違う注記を紛れ込ませたことなので、注記を外し、
**「この節だけは意図的に動くブランチを見る」理由**と、
**再現したいときは 6.7（タグ固定）を使う**という導線を明示した。

非公開側は 5 巡目の新規指摘なし（Devin は 6.7 の使い分けを「矛盾ではない」と確認）。

### GitHub 側 AI レビュー（6 巡目）

| # | 指摘元 | Severity | 内容 | 対応 |
|---|--------|:--:|---|---|
| K1 | Codex bot (P2) | **重要** | 2.6.1 の拒否対象を **CHANGELOG 日付（2026-08-13）で線引き**していた。実際の判定基準は**そのワークフローを作ったシェルの state スキーマが v8 より前かどうか**であり、2.6.1 公開後も古いシェルを使い続けた環境では**それ以降に作ったものも対象になる** | ✅ 3 ファイルで判定基準を state スキーマに置き換え、日付で数えないよう明記 |

**K1 は本改訂で 2 度目の同型の誤りである。**
「実際の基準（state スキーマの版）」の代わりに「観測しやすい代理値（リリース日）」を書いた。
[15 章 15.11](./15-release-impact-26123.md#1511-版番号の同定について--ズレが再発した) が
「版の判定は `CHANGELOG.md` の見出しのみで行う」と書いているのは版の**同定**の話であって、
**影響範囲の判定を日付でやってよいという意味ではない**。
日付は代理値であり、環境ごとに導入時期が違えば代理として成立しない。

### GitHub 側 AI レビュー（7 巡目）

| # | 指摘元 | Severity | 内容 | 対応 |
|---|--------|:--:|---|---|
| L1 | Copilot | 中 | **非公開 README の警告バナー**が「`/tree/v2` が残っていると動かない」と断定しており、他文書の「302 転送」と矛盾 | ✅ 3 通り（止まる／静かに古いまま動く／普通に開ける）に整理 |
| L2 | Copilot | 中 | 公開 README のクイックスタートに付けた版固定コメントが、手順の意図を曖昧にしていた | ✅ 「導入するだけなら `main`／再現・照合ならタグ」と用途で書き分け |

**L1 も同型の取りこぼしである。** 302 転送の実測を 16 章・§2・申し送りには入れたが、
**警告バナーだけ直っていなかった。** バナーは最初に読まれる場所なので、
ここが古いままだと後段を読んでもらえない可能性がある。

### GitHub 側 AI レビュー（8 巡目）

| # | 指摘元 | Severity | 内容 | 対応 |
|---|--------|:--:|---|---|
| M5 | Codex bot (P2) | 中 | 原因別内訳の合計が古いまま（M1〜M3 を足しても 39 のまま）。M2・M3 はコピー同期漏れなのに 9 件の側に入っていない | ✅ 表の行を機械で数え直し、数え方も本文に明記した |
| M4 | Codex bot (P2) | 中 | 16.3 に書いた `git fetch origin 2fbee12f`（短縮 SHA）は **動かない**。refspec として解釈され `fatal: couldn't find remote ref` になる | ✅ 実測して確認。`--unshallow` か **40 桁の完全な SHA** に訂正 |
| M3 | Codex bot (P2) | 中 | 「実装が動いたのは 2.6.124 のみ」が **5 ファイル**に残っていた。同じ文が直前で「2.7.0 はバージョン定数を変えた」と書いており矛盾する（定数もまた実装である） | ✅ 5 件すべてを「**ロジック**が動いたのは」に統一。今回は**先に全コピーを列挙してから一括で**直した |
| M2 | Codex bot (P2) | 中 | 非公開側に「2.7.0 にコード変更は無い」が **2 箇所**残っていた（G2 の機械置換は「コード変更ゼロ」だけを対象にしており、言い回しの違う 2 箇所を拾えていなかった） | ✅ 訂正 |
| M1 | Copilot | 低 | 非公開 `reports/harness-vs-aidlc_v2-comparison.md` の表セルで、`IMPACT-*` 参照の 2 件目以降に `analysis/` プレフィックスが無い（`reports/` からの相対パスとして存在しない） | ✅ 全件に付与 |

**公開側は Copilot が *Approval recommended*。** 8 巡目で新規の実質指摘は上記 1 件のみとなった。

### 却下した指摘

| 指摘元 | 主張 | 裁定 |
|--------|------|------|
| Copilot | `10-release-impact-*.md` は実在しないので TRACEABILITY の `10-`〜`16-` は誤り | **却下**。`10-release-impact-2537.md` は実在する。レビュー用パケットに全ファイルを含めなかったための誤認 |
| Cursor | release-impact 章は `11-` からなので範囲表記を `11-` に直すべき | **却下**（同上） |
| Copilot | `v1` の HEAD SHA・タグ時刻・`release.yml` の内部実装・Mermaid 修正の動機などは一次事実に無く未検証の断定 | **却下**。いずれも上流クローンで直接確認済み（Mermaid の動機は `518578ad` のコミットメッセージの逐語）。ただし **ISSUE_TEMPLATE の由来だけは実際に誤り**だったので採用（上表 #5） |
| Copilot | CHANGELOG 日付 2026-09-01 は未確認 | **却下**。`## [2.7.0] - 2026-09-01` を直接確認済み |
| Codex bot (P2) | `blob/v2/...` の URL はもう解決しない | **却下**。実測すると 302 で `main` に転送される。Devin の観察が正しい |
| Devin bot 🔍 | 変更行に末尾空白が残っている（`git diff --check` が報告） | **却下**。Markdown の強制改行（行末 2 スペース）で、本リポジトリ全体で従来から使っている記法である。差分検査の側を調整するのが筋 |

### オーケストレータの判定

- **ブロッキング HIGH: 0**（HIGH 3 件はいずれも本改訂内の記述誤りで、**すべて修正済み**）
- 過去章の日付付き測定記録は本文を 1 行も書き換えていない（Cursor が明示的に確認）
- 内部リンク・アンカーは機械検査で全解決
- 公開リポジトリの機密語スキャン: **残存 0 件**
- 8 系統中 **6 系統で実施**（Devin は再実行で完了、Kiro は本環境で非対話出力不可、Grok は除外）
- **GitHub 側 2 巡目**: Copilot が公開側で *Approval recommended* → 追加指摘で *Changes recommended*、
  非公開側は *Changes recommended*。Codex bot が P1 1 件・P2 2 件、Devin bot が両 PR で計 9 件。
  **うち 10 件を採用（上表 G1〜G10）、1 件を却下。**
  最も重かったのは **`/aidlc plugin sync` の脱落**（G3）で、これは読者を実害に導きうる欠落だった。
- **GitHub 側 3 巡目**: 6 件採用・1 件却下。**うち 3 件（H1〜H3）は 2 巡目の修正が同じ事実の別コピーに
  波及していなかったもの**で、指摘の内容が新しかったわけではない。とくに H3 は、
  レポート本体だけ直して**申し送り文書を直し忘れた**もので、申し送りは runbook に転記される前提である以上、
  実害の距離はこちらのほうが近い。
- **GitHub 側 4 巡目**: 3 件採用。1 件は**実質的な事実誤り**（止まる契機を「同上」で潰した）で、
  残る 2 件は自分が持ち込んだ矛盾（404 と 302）と、公開側に波及していなかった版固定である。
- **4 巡を通じた反省**: 新規に書いた内容そのものより、**同じ事実の別コピーを直し忘れた**ことと、
  **要約のつもりの省略が事実を痩せさせた**ことのほうが、指摘の数としても実害としても大きかった。
- **収束**: GitHub 側は 8 巡。実質指摘は 1 巡目 10 件 → 8 巡目 1 件まで落ちた。
  公開側は最終的に Copilot *Approval recommended*。
- **原因別の内訳**（手元 **15 件** + GitHub 側 **28 件** = 採用 **43 件**。本改訂時点）。
  **この欄は 4 回書き直している** —— 過少分類 → 合計の取りこぼし（36→39）→
  M 系の追加を反映し忘れ（39→43）→ 今回。**集計を直す文章で 3 回続けて集計を間違えた。**
  以後は手で数えず、下の方法で数え直すこと。

  ```
  採用表の行 = 先頭セルが #数字 / G / H / I / J / K / L / M で始まる 5 列の表の行
  （冒頭にある 2026-07-28 の Devin 表の #1-#4 は対象外）
  ```

  | 原因 | 件数 | 該当 |
  |---|---:|---|
  | **一次情報の確認漏れ・事実誤認** | **9** | #1 #3 #4 #5 G1 G3 G4 G5 M4 |
  | **要約・代理値による事実の欠落** | **2** | I1（「同上」で圧縮）K1（判定基準を日付に置換） |
  | **集計・計算の誤り** | **3** | #2（76≠74）#15（「124 版分」）M5（この欄） |
  | **同じ事実の別コピーの直し忘れ** | **11** | #12 G2 H1 H2 H3 H4 I2 I3 L1 M2 M3 |
  | 表現・体裁・編集ミス | 18 | 残り |

  合計 9+2+3+11+18 = **43**。

  **最多はコピー同期漏れ（11 件）だが、確認漏れ（9 件）とほぼ拮抗している。**
  対策をどちらか一方に寄せると、もう一方が丸ごと残る。必要なのは 4 つで、それぞれ別の作業である。

  1. **書く前に一次情報へ当たる**（9 件）—— コマンドは実行してから書く。M4 はその典型で、
     `git fetch origin <短縮SHA>` を実行せずに書いて動かなかった。
  2. **書いた後に全コピーを機械照合する**（11 件）—— ただし **grep は書いた文字列しか見つけない**。
     G2 は「コード変更ゼロ」で置換して「コード変更は無い」を取りこぼし（M2）、
     M3 は「実装が動いた」で置換して「実装が動いた唯一の版」を取りこぼした。
     **同義語の候補を先に列挙してから走らせること。**
  3. **数を書いたら機械で数え直す**（3 件）。
  4. **レビュー結果を全件取得する** —— GitHub API を `--paginate` 無しで叩いていたため、
     非公開側の指摘を 1 ページ目しか見ておらず、3 件を「新規指摘なし」と誤報告した。
     **取りこぼす読み方をしていては 1〜3 を守っても意味がない。**

- **判定: APPROVE**（マージ可。ユーザーの承認を得てマージ）

---

## 2026-09-09 — 上流 2.7.0 → 2.8.1 追従（`dist/` 廃止・ネイティブ配布）

対象: 新規章 `17-release-impact-2801.md` ＋ 両リポジトリの既存記述の改訂
（内部側は `analysis/IMPACT-2700-to-2801.md` ＋ 改訂差分。機密識別子はマスクして送付）

### 実施系統（8 系統中 6 系統）

| 系統 | 実施 | 結果 |
|---|---|---|
| self（Claude） | ○ | 事実収集をマルチエージェント 5 系統で実施。数値はすべてコマンドで再測定 |
| Codex | ○ | HIGH 4 / MEDIUM 2 / LOW 1。**算術不整合を最初に検出** |
| Cursor | ○ | HIGH 4 / MEDIUM 3 / LOW 2。**Codex と重ならない HIGH を 3 件検出** |
| Copilot | ○ | HIGH 1 / MEDIUM-HIGH 1 / MEDIUM 1 |
| Antigravity | ○ | HIGH 2 / MEDIUM 2 / LOW 2 |
| Devin | ○（再実行） | HIGH 1 / MEDIUM 3 / LOW 4。初回は workspace trust で拒否され、信頼済みディレクトリから再実行 |
| Kiro | ✗ | 本環境の CLI が非対話出力に非対応（前回と同じ。出力 0 バイト） |
| Grok | ✗ | ユーザー指示により除外 |

### 採用した指摘（すべて修正済み）

| 重大度 | 指摘 | 検出 | 対応 |
|---|---|---|---|
| HIGH | 変更内訳表の合計が全体値と一致しない（12 ファイル欠落） | Codex / Cursor / Copilot / Antigravity | **実測で「その他 12 / +810 / −485」を確認**し、両文書に追加。合計行も追加して機械照合可能にした |
| HIGH | 「中核メトリクスは 1 つも動いていない」が同じ表の `core/tools 51 → 69` と矛盾 | Codex / Cursor | 「**ワークフロー体系の指標は不変／中核表のうち `core/tools` は動いた**」に統一。**同型表現の全 9 コピーを機械置換で同期** |
| HIGH | 17.11「16 章までの測定値は 2.8.1 でもすべて有効」が `core/tools` と矛盾 | Cursor | 「記録として有効」と「現在値として引用してよい」を書き分け |
| HIGH | `01-overview.md` の版チェーンから 16 章リンクが欠落（改訂時に置換してしまった） | Cursor | 16 章を復元し 17 章を追加 |
| HIGH | 内部 `REMAINING_TASKS` C 節が「in-place upgrade は無い」の旧前提のまま | Cursor | 節冒頭に前提の失効を明記。`aidlc update --check` の存在を反映 |
| HIGH | `02-architecture.md` の「51 は CLI の本数ではない」注記が 2.7.0 のまま | Antigravity | 69 / CLI 39 / ライブラリ 30 に更新。比較表も 3 列に拡張 |
| HIGH | 17.10 の `f9f48945` 行が「17.7 の暫定コメントアウトの解除」と読める | Antigravity | **入れたコミットであって解除ではない**旨に修正 |
| HIGH | 2.8.0 の「実装コミット」列が `v2.8.0` タグの指すコミットと食い違う | Codex / Cursor / Devin | 列名を「CHANGELOG 投入コミット」に変更し、**タグは `0d399dd8` を指す**旨を注記 |
| HIGH | 版固定で `v2.8.0` と SHA `c03f9e28` を同列に並べている（用途が違う） | Codex / Cursor / Devin | **導入固定 → `v2.8.0` / ソース照合固定 → SHA** と用途を分離 |
| MEDIUM-HIGH | 「`harness/` は実質すべて呼び出し文字列の書き換え」が §4 と矛盾 | Copilot | `harness/claude/settings.json` を明示的な例外として併記 |
| MEDIUM | 上流 roadmap の陳腐化を「4 版古い」と記載 | Copilot | **実測して 5 版**（2.7.0 / 2.7.1 / 2.7.2 / 2.8.0 / 2.8.1）に訂正 |
| MEDIUM | 「恒久的に無効化できる」が不可逆と誤読される | Codex | 「**設定ファイルに記録して無効化できる**」に統一。書き換え可・env 最優先を明記。全コピーを機械置換 |
| MEDIUM | 「任意のシェルコマンドは権限プロンプトを経る」は過大主張 | Codex / Devin | 「**出荷設定で自動承認される範囲が狭まった**。最終的な挙動はホスト側設定にも依存」に限定 |
| MEDIUM | 内部 §7「4 版ともランタイム挙動の変更なし」が 2.7.1 と衝突 | Cursor | `review_artifact` 要件の側に限定し、2.7.1 は挙動を変えている旨を明記 |
| LOW | 「挙動そのものを直したのは 2.7.1 だけ」の根拠範囲 | Codex | 「**本調査で確認できた範囲では**」に限定し、全 hunk 精読ではない旨を明記 |
| LOW | 「`core/` の増加はほぼ全量が新規 18 本」は未計測の断定 | Cursor | **`git diff --numstat` で実測**し「18,393 行（83%）」に置換 |
| LOW | Codex trust 手順を「`aidlc config trust` でも確認できる」に弱めてしまった | Cursor | 具体コマンドを復元。**ネイティブ導入時の生成手段は未確認**と明記 |
| LOW | 6.1 表ヘッダ「導入元」が導入コマンドを載せている | Antigravity | 「導入コマンド（2.8.x）」に変更 |
| LOW | `import.meta.main` 保持数が 02 章と 17 章で不一致 | Devin | 02 章を 39 に更新して整合 |
| LOW | `resolveProjectFlag()` の「同じ経路」が不正確 | Devin | バイパス 9 種は `flags.bypasses`、他 4 つは `PROJECT_FLAG_FIELDS` と**参照先の違いを明記** |

### 却下した指摘（根拠を実測して確認）

| 指摘 | 却下理由（実測） |
|---|---|
| `core/tools/aidlc-init.ts:6523,6531` の行番号が大きすぎる（要確認） | **実在する。** `wc -l` = 6,549 行。両行に `dist/<harness>/` 再コピーの文言が実在 |
| `AIDLC_DISABLE_PLAN_APPROVAL_GUARD` が「承認を復旧しない」は根拠不足（要確認） | **確認済み。** 当該変数の読み出しは `core/hooks/aidlc-plan-approval-guard.ts:630` の **1 箇所のみ**。`beginCodeGeneration`（`aidlc-testing-posture.ts:1716`）は環境変数と無関係に throw する |
| （事実収集エージェント）記録値「エージェント 11 / スコープ 6」は誤り | **却下。** 当方の記録は **エージェント 14 / スコープ 11** で実測と一致。エージェントへ渡した私の要約側の対応付けが誤っていた（記録側に誤りなし） |

### 原因の内訳（20 件）

> 集計規則: 上表「採用した指摘」の行数を数える。同一事象を複数系統が検出した場合も 1 件とする。
> 手で足さず `grep -c` で数えること。

| 原因 | 件数 | 該当 |
|---|---:|---|
| **同じ事実の別コピーの不整合** | **7** | 内訳表合計 / 不変主張の矛盾 / 17.11 / `02` の CLI 本数 / `harness` 例外 / `import.meta.main` / 内部 C 節 |
| **改訂時の取りこぼし・置換事故** | **3** | `01` の 16 章リンク欠落 / Codex trust 手順の劣化 / 6.1 表ヘッダ |
| **過大主張・断定の行き過ぎ** | **4** | 恒久的に無効化 / 任意のシェルコマンド / ほぼ全量 / 2.7.1 だけ |
| **集計・計算の誤り** | **2** | 12 ファイル欠落 / roadmap 4 版 → 5 版 |
| **記述の曖昧さ（誤操作を招く）** | **3** | 2.8.0 の実装コミット列 / 版固定の用途混同 / 内部 §7 の限定不足 |
| **精度不足** | **1** | `resolveProjectFlag` の参照先 |

**前回（43 件）から 20 件へ減少した。** ただし最大要因は変わらず「**同じ事実の別コピー**」（前回 11 / 今回 7）である。
今回は言い換え語を先に列挙してから機械置換する手順を取ったが、
**自分で書いた文と、エージェントに挿入させた文で表現が割れて 1 件取りこぼした**
（`IMPACT-2649-to-2655.md`。最終照合の grep で検出）。
**挿入文言は 1 か所で定義して全箇所に配る**運用にすべきである。

### 申し送り（GitHub PR レビューへ回さず記録に留めた事項）

いずれも**手元での追加ラウンドでは確度が上がらない**ため、実機・社内確認の課題として記録した。

- ネイティブインストーラの実機実行（`install.sh` / `install.ps1` の導入結果）
- `aidlc config` がプラグイン compose フックを自動実行するか
- Windows ARM64 でのネイティブバイナリの挙動
- `v2.8.1` タグの付与時期（付くまで 2.8.1 の修正は検証不能。**基準時点で 2 件、その後 3 件に増えた**）
- 上流のリリース時フルテストスイート無効化がいつ解除されるか

### GitHub PR レビュー（PR #12 / internal-eval #14、2026-09-09）

Devin Review / Copilot / Codex の 3 ボットから**インラインコメント 18 件**。
重複を統合すると **15 件の指摘**で、**すべて採用**した（コメント ID を機械照合し、未対応 0 件を確認）。

| 重大度 | 指摘 | 検出 | 対応 |
|---|---|---|---|
| 🔴 | **17.10 の表が引用ブロックで分断され、2.7.2 / 2.7.1 行が表として描画されていなかった** | Codex | 注記を表の後ろへ移動。**両リポジトリの全 Markdown 表を機械走査**して同種の崩れが無いことを確認 |
| 🔴 | **内部 `docs/REMAINING_TASKS.md` の 3 件の編集がそもそも適用されていなかった** | Codex | シェルのクォート解析エラー（`unmatched "`）でコマンドが落ち、`ok` を確認しないまま次へ進んでいた。Python をファイル経由で実行し直し、**適用後に grep で検証** |
| 🟡 | 09 章・6.7 の版固定が `v2.8.0` のみで、2.8.1 のソースを照合できない | Devin | **用途別に分離**（リリース資産 → `v2.8.0` / 本ノートのソース照合 → `c03f9e28` を checkout） |
| 🟡 | 6.5 の直接ツール呼び出しが `bun` 前提のままで、ネイティブ導入者は実行できない | Devin | `workspace-sync` は上流が案内する **`aidlc system workspace-sync`** に更新。**`codekb-scope-diff` は上流が今も `bun` 形式のままである**ことを実測し、`engine` 名前空間が「not for human scripts」と明記された hidden ルートである点を含めて記載（**ネイティブ導入だけの環境では公式手段が無い**旨を未確認事項へ） |
| 🟡 | 内部 `reports/aidlc-v2-customization-evaluation.md` が `plugin sync` を無条件必須と断定 | Devin | **上流の記述が 2 つに割れている**ことを実測（`12-cli-commands.md` は「毎回」だがその理由は `dist/` コピー前提。`18-install-and-lifecycle.md:818-820` は「`aidlc config` に収束」「`doctor` が状態を報告」）。**「`doctor` で確認 → ずれたら sync」**に改めた |
| 🔍 | `02-architecture.md` の技術スタック表が bun を全ハーネス共通の実行前提としたまま | Devin / Codex | ビルド時と配布版実行時を分離 |
| 🔍 | `02-architecture.md` の Claude 行がモデル pin を出荷既定としたまま | Codex | 削除を反映（**版の帰属は後のラウンドで 2.8.1 → 2.7.2 に訂正**） |
| 🔍 | `08-v1-vs-v2.md` の「向かない」に bun 制約が残存 | Devin | ソース生成時のみの制約に限定 |
| 🔍 | 内部 `harness-vs-aidlc_v2-comparison.md` の統合見送り理由が Bun セットアップコスト前提 | Devin | **①の判断自体は他の 2 理由で維持**としつつ、本項を根拠に引用する場合は再評価が要る旨を追記 |
| 🔍 | 内部モデル劣化レポートが Opus 4.8 を「推奨・既定」と併記 | Devin | **「推奨」（README）と「出荷既定」を分離**。出荷 pin は **2.7.2** で削除された（後のラウンドで版を訂正） |
| 🟡 | 内部 `REMAINING_TASKS` の `dist/codex` / `dist/opencode` / `dist/cursor` が未修正 | Codex | 検証対象パスを `harness/<name>/` ／ `runtime/<harness>/` ／導入済み実配置に読み替え |
| 🟡 | 内部 comparison の統合ステップ 1 が `dist/claude/.claude/settings.json` を編集対象としたまま | Codex | **導入済みプロジェクト内の `.claude/settings.json`** に変更。出荷内容を変えるならフォーク運用になる旨も明記 |
| ⚪ | 6.3 の 2 段階手順に「確認する」行が無く、パイプ直結と実質同じ | Copilot | 確認行を追加し、確認を挟まないなら同じである旨を明記 |
| ⚪ | `SOURCES.md` / `docs/TRACEABILITY.md` の章番号レンジが 15 / 16 章止まり | Copilot | 17 章まで拡張。**16 章側の「11〜15 章」は 2026-09-01 時点の凍結記録なので据え置き** |
| ⚪ | `aidlc config --harness <name>` の表記が資料間で不統一 | Copilot | `--harness` が省略可であることを注記して差を説明 |

**この 15 件のうち 10 件が「同じ事実の別コピー」**であり、手元 8AI で 7 件直した後にまだ残っていた。
**手元レビューはパケットに入れた差分しか見ない** —— 差分に現れない既存記述（`02` の技術スタック表、
`08` の比較表、内部レポートの判断根拠）は、リポジトリ全体を見る GitHub 側でしか出なかった。

**最も重かったのは編集の適用漏れである。** シェルのエラーで 3 件の編集が落ちたことに気づかず、
「反映済み」と書いた表まで作っていた。**編集は適用後に必ず grep で検証する。**

#### 再レビュー 第 2 ラウンド（同日）

**push 後に Devin から新規 2 件。** 2 件は解決済みと判定された。

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | **内部モデル劣化レポートの「運用で必ず確認すべきこと」「管理上のポイント」が、`AIDLC_SKIP_HUMAN_PRESENCE_GUARD` を環境変数だけ確認する手順のままだった** | **実害のある見落とし。** 2.8.x ではこの変数が設定ファイルからも解決されるため、環境変数だけ見た管理者は**無効化されているガードを有効と誤判定する**。4 層すべての点検手順と `aidlc config flags --show` を追記 |
| 🔍 | 内部 §8.3 の見出しが「プラグイン利用時は YES」のまま、本文の「doctor で確認 → ずれたら sync」と食い違っていた | 見出しと §8.2 の手順を本文に揃えた。公開ノート 6.4 のバナーにも同じ手順を追記 |

**1 件目で過大主張を避けられた。** 上流を実測したところ、
レポートが挙げる 3 変数のうち**設定ファイル記録の対象は `AIDLC_SKIP_HUMAN_PRESENCE_GUARD` だけ**で、
`AIDLC_SKIP_REVIEWER_GATE_GUARD` と `AIDLC_UNATTENDED` は `process.env` 直読みのままだった。
**「3 つとも設定ファイルで無効化できる」と書いていたら、それ自体が新しい誤りになっていた。**

**待った価値があった。** 1 ラウンド目の対応を push した直後に、`CLEAN` ではなくこの 2 件が届いた。
→ [[pr-merge-wait-for-rereview]] の再確認。

#### 再レビュー 第 3 ラウンド（同日・Codex 完了）

**Codex から新規 2 件。いずれも「失敗様式の取り違え」で、私の断定が実測を越えていた。**

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | `04-agents.md` が「Composer への `@<server>` 追加はエンジン更新で**消える**」と断定 | **2.8.x では失敗様式が違う。** `aidlc config` は所有権ベースラインを持ち、記録済み SHA-256 と一致しない managed ファイルを**拒否して conflict として報告する**（`doctor` も conflict に分類）。`--force` を付けた場合に置換される。**「再付与」ではなく「衝突の解消」**が正しい手順であると書き直し、2.7.0 までとの対比表にした |
| 🟡 | 内部 comparison の統合ステップが「追記は上流更新で消える」と断定。**新規に足した deny は user-owned JSON value なので `--force` でも上書きされない可能性がある** | **上流ドキュメントから確定できないことを実測で確認した。** 「Root Integrations and Ownership」表は部分所有の面を 5 つ列挙しているが、**`.claude/settings.json` はその表に載っていない**。よって user-owned JSON value 扱いか managed harness file 扱いかは判別できない。**どちらにも断定せず未確認とし、`aidlc config --dry-run --json` の planned action（`preserve` / `merge` / `update`）で確認する手順**を示した |

**2 件目で、Codex の前提にも寄りかからずに済んだ。**
Codex は「user-owned なので保存される」と推定していたが、上流の該当表を実測すると
**`.claude/settings.json` は列挙されておらず、どちらとも決まらない**。
**「私の断定が誤り」と「相手の断定が正しい」は別である。** → [[dont-overcorrect-into-the-opposite-claim]]

修正の過程で、**自分の文章内の矛盾も 1 件見つかった** ——
同じ `.claude/settings.json` を、上では「V2 が導入した管理対象」、下では
「フレームワーク管理外なので入れ替えの影響を受けない」と書いていた。

#### 再レビュー 第 4 ラウンド（同日）

**新規 13 件。うち 1 件は本追従で最も重い誤りだった。**

| 重大度 | 指摘 | 検出 | 対応 |
|---|---|---|---|
| 🔴 | **モデル固定・`effortLevel`・無制限 `Bash`・`templated` tier の削除を「2.8.1 の変更」と書いていたが、実際は 2.7.2（`12b8d6e0` / #756）** | Devin | **`git log -S` で削除コミットを特定して確定。** 2.7.2 は公開済みの **v2.8.0 に含まれる**ため、**2.8.0 利用者にはすでに効いている**。「2.8.1（未リリース）の変更」と読むと **pin がまだ効いていると誤解する**。両リポジトリの全コピー（8 箇所）を訂正 |
| 🟡 | 初回ウィザードを「常に 6 ステップ」と説明。実際は**検出から始まり、1 種検出時は 3 択**（推奨既定 / 6 ステップ / 終了） | Devin | 上流 `18-install-and-lifecycle.md:231-243` を実測し、検出結果ごとの分岐表を追加。**6 ステップはカスタマイズ経路**と明記（notes 6 章・17 章の両方） |
| 🟡 | **`--force` は所有権ベースライン登録済みのファイルにしか効かない。** 2.7.0 の `dist/` からコピーして手編集したファイルは「曖昧な未登録内容」として拒否され、`--force` でも置換できない | Devin | 上流の逐語（"cannot adopt ambiguous unmarked content"）で確認。**出自で分けた表**にし、移行ケースは「退避して移動／削除し `aidlc config` に作り直させる」が正しい手順であると明記 |
| 🟡 | **`aidlc config flags --show` の通常表示は環境変数だけのバイパスを表示しない** | Devin | `core/tools/aidlc-init.ts:2568` を実測（human 出力は `resolved.flags?.bypasses` を反復）。**`--show --json` の `effective` / `sources` を見る**手順に訂正。**前ラウンドで私が入れた対策自体の誤りだった** |
| 🟡 | `.claude/settings.json` への `permissions.deny` 追記の扱いを「未確認」としたが、**実際は判定できる** | Devin | `scripts/package.ts:579-582` で `managedDirectories` が投影のトップレベルディレクトリ全部（root integration 以外）であり **`.claude/` が含まれる**ことを確認。`aidlc-init.ts:5039` に `conflict` /「`locally modified or unowned`」が実在。**通常は conflict、`--force` で `update`（追記は失われる）**と確定して記載 |
| 🟡 | `dist/` コピー方式の境界を「2.8.x 以前」としていたが、**2.7.2 の Upgrade 文はすでにネイティブ経路を案内している** | Codex | 境界を **2.7.1 以前**に訂正（両リポジトリ 4 箇所） |
| 🟡 | §7 サマリが Opus 4.8 を「推奨・**既定**」としたまま | Codex | 前ラウンドで別行だけ直しており、**サマリ行が残っていた**。「推奨（README）」と「出荷既定ではない」を分離 |
| 🟡 | 09 章の「タグで固定する」が未リリース版に成立しない | Codex | 「**タグまたはコミット SHA に固定する**」に変更 |
| 🟡 | 「2.8.1 の修正は動作を確認する手段が現時点で無い」は過大 | Codex | SHA を checkout すればソースからの検証は可能。**「公開ネイティブ資産では確認できない／本調査では未検証」**に限定 |
| 🟡 | 「2.8.0 は 2.7.2 から挙動変更なし」が、タグに含まれる #1049 / #1050 の挙動変更と矛盾 | Codex | **CHANGELOG エントリについての話**であると限定し、タグ実体には 2 件の挙動変更が含まれると明記 |
| 🔍 | plugin sync の上流不整合は**とくに Kiro CLI で実測が要る** | Devin | Kiro CLI だけ SessionStart 自己修復が効かないため結果に直結する旨を未確認事項に追記 |
| 🔍 | 2.8.1 の配布動作はタグ公開後の再確認が要る | Devin | 未確認事項に記録済み（変更なし） |
| 🔍 | プラグイン更新の正式経路は上流確認が要る（暫定手順は妥当） | Devin | 同上 |

**版の帰属を誤ったのが最も重い。** HEAD（2.8.1）で測って 2.8.1 の変更として書いた、という
**過去に指摘済みの失敗様式そのもの**である（→ [[measured-at-revision-is-not-introducing-version]]）。
**実務上の意味が逆になる誤りだった** —— 「未リリースの 2.8.1 の話」と読めば pin がまだ効いていることになるが、
実際は 2.8.0 を使っていればすでに解除されている。
**変更を書くときは、測った版ではなく `git log -S` で入った版を特定する。**

**前ラウンドで自分が入れた対策が誤っていた例も 1 件**（`flags --show`）。
**対策として書いたコマンドも、実行結果を確かめてから書く。**

**レビュアーの測定が自分の「未確認」より正確だった例も 1 件**（`.claude/settings.json`）。
**「確定できない」と書く前に、もう一段ソースを追う。**

#### 再レビュー 第 5 ラウンド（同日）

**新規 5 件（ほかに解決確認 5 件）。全件対応。**

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | 6 章の版境界が「2.7.0 まで／2.8.x」で、**2.7.1 と 2.7.2 の扱いが抜けていた** | **実測して整理し直した。** 配布方式の変更が入った実装版は **2.7.2**。**ただし `v2.7.1` / `v2.7.2` のタグは存在しない**（リモートのタグは `v2.7.0` と `v2.8.0` のみ）。したがって**実装版の境界は 2.7.1／2.7.2、リリースの境界は v2.7.0／v2.8.0** になる。両方を明示した対照表を 17.1 に置き、6 章・5 章・2 章の表現を揃えた |
| 🔍 | `docs/REMAINING_TASKS.md` に「動作を確認する手段が無い」が残存 | 17.12 だけ直して**未確認リスト側を直し忘れていた**。「公開資産では確認できない／本調査では未検証」に統一 |
| 🔍 | `REVIEW-8AI.md` の見出しが「8 系統中 5 系統」なのに表は 6 系統 | 見出しを 6 系統に訂正（**自分のレビュー記録の集計誤り**） |
| 🔍 | 内部 comparison の概要表が「Opus 4.8 + Bedrock デフォルト」のまま | 測定時点（2.5.37）の値であることと、**2.7.2 以降は出荷固定が削除されセッション継承**である旨を併記 |
| 🔍 | 内部 comparison の要約が「恒久記録」表現のまま | **「永続的に記録」に統一**。設定は書き換え・削除でき環境変数が最優先である旨も併記。全コピーを機械置換 |

**版境界の誤りが 2 ラウンド連続で出た。** 前ラウンドは「2.8.1 → 2.7.2」、今回は「2.7.0 まで／2.8.x」。
**実装版とリリースが 1 対 1 でない区間では、境界を書くたびにどちらの観点かを明示する。**
`v2.7.1` / `v2.7.2` のタグが無いという事実を最初に押さえていれば、両方まとめて防げた。

**追加 2 件（同ラウンド、Codex）:**

| 指摘 | 対応 |
|---|---|
| `CONVERSATION_LOG.md` が「ロジックが動いたのは 2.7.1 のみ」と要約。**リリースされた v2.8.0 には #1049 / #1050 の挙動変更が含まれ、2.8.1 にも 2 件ある** | 「**ワークフロー実行系の**ロジック」に限定し、配布・インストール系が別に動いている旨を併記 |
| 6 章に「2.8.x」表記が残り、2.7.2 の読者を旧手順へ誘導しうる | 実装版の主張（hook 起動形式・配布物の入手方法・bun 不要・測定手法・`workspace-sync`・PDF の所在）を **2.7.2 起点**に統一。あわせて**章の冒頭に「本章で単に 2.8.x と書くのはリリース観点」**と明記 |

#### 再レビュー 第 6 ラウンド（同日）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | 「出荷時のモデル固定が削除され、セッション設定を継承する」が**広すぎた**。**`balanced` tier は今も固定されている** | **実測して確認**（`balanced` は 2.7.0 と HEAD でバイト同一）。Claude `sonnet`/`medium`、Codex `openai.gpt-5.6-terra`/`medium`、opencode Bedrock Sonnet/`medium` を今も固定する。上流コメントは `balanced` を「**レビュア tier。レビュー専用エージェント 2 体だけが持つ**」と説明している。**削除されたのはトップレベル pin と `templated` のみ**と範囲を限定し、投影表を両リポジトリに追加 |

**モデル劣化レポートの論点に直結する見落としだった。**
「モデル固定が消えた」で止めると、**レビュー工程が今も Sonnet / medium で走る**という
本レポートの中心的な関心事が消える。
**「変わった」と書くときは、変わっていない部分を同じ文で示す。**

#### 再レビュー 第 7 ラウンド（同日）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | **`balanced` の固定を全ハーネスに一般化していた。** 実際に固定があるのは Claude Code / Codex CLI / opencode の 3 系統で、Cursor / Kiro / Copilot はセッション継承 | ハーネスを明示して限定。`02-architecture.md` の Claude 行にも `balanced` の固定が残る旨を追記 |

**前ラウンドで自分が載せた投影表が、直後の結論文を否定していた。**
表には Cursor / Kiro / Copilot が「固定なし」と書いてあるのに、
結論は「レビュー工程は今も Sonnet / medium で走る」と限定なしに書いていた。
[[one-fact-many-copies-sync-them-mechanically]] に記録した
**「自分の表が自分の要約文を否定している」型がまた出た。**
**表を書いたら、その直後の結論文を表に照らして読み直す。**

#### 再レビュー 第 8 ラウンド（同日）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | モデル劣化レポートの追記で、**`templated` の投影値（`inherit` / `null`）が `balanced` の箇条書きの下にぶら下がっていた** | 位置の誤りにより、**レビュア用エージェントがセッション継承すると読める**状態だった。2 つの tier を別々の箇条に分け、**両方の投影値を明記**した |

**挿入位置の誤りだけで、意味が反転しうる。**
文言そのものは正しかったが、`balanced`（固定）の説明の直後に `templated`（継承）の値を置いたため、
**前ラウンドで直したばかりの論点が読み手には元に戻って見える**状態になっていた。
**注記を差し込むときは、それがどの箇条の続きとして読まれるかを確認する。**

#### 再レビュー 第 9 ラウンド（同日）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | **手動コピー経路を「ネイティブ導入の代替」として並べていた。** `runtime/<harness>/` の投影は**ネイティブ `aidlc` を呼ぶ形**で、**tar.gz にバイナリ本体は入っていない** | 上流 `README.md:33-36` が「**Install the matching native `aidlc` command**, download …, and copy `runtime/<harness>/`」と**バイナリ導入を先に置いている**ことを確認。**代替ではなく前提**であると明記し、両リポジトリの経路表と手動コピー節に警告を追加 |

**自分の文章内に答えが書いてあった。** 同じレポートが数行下で
「`dist-release/` はネイティブ `aidlc` を呼ぶ形に書き換えられている」と説明しているのに、
経路表では「ネイティブ導入 / アーカイブのコピー」を**並列の選択肢**として並べていた。
**選択肢を表にするときは、各行が前提を共有しているかを確認する。**

#### 再レビュー 第 10 ラウンド（同日）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | 手動コピー手順が**バイナリとアーカイブの版一致を求めていなかった** | 上流は「Install the **matching** native `aidlc` command」と書いている。`releases/latest` で入れたバイナリと別リリースのアーカイブを組み合わせると版がずれる。**両方に同じ `X.Y.Z` を指定する**手順と、`aidlc version` での確認を追加 |
| 🟡 | **前ラウンドの訂正が `docs/REMAINING_TASKS.md` に同期されていなかった** | 現行タスクが移行先を「ネイティブ導入 / アーカイブのコピー」の二択のまま提示しており、**意思決定用の文書が誤った二択を示し続けていた**。**「バイナリ導入はどちらでも必須。選ぶのはプロジェクト内ファイルの管理方法」**という枠組みに書き直した |

**2 件目はこのセッションで 3 度目の同型**（新章／レポートを直して、判断に使う一覧を直し忘れる）。
**訂正を入れたら、その事実を根拠に意思決定する文書を必ず名指しで確認する。**
今回で言えば `docs/REMAINING_TASKS.md` が「どちらを社内標準にするか決める」という
**判断そのものを載せている文書**だったので、優先度は新章より高かった。

#### 再レビュー 第 11 ラウンド（同日・Codex）

**新規 3 件。3 件とも「自分の文書内の矛盾」だった。**

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | 「再コピー」の読み替え先を一律 `aidlc update` としたが、**2.7.1 以前の導入にはネイティブ `aidlc` が存在せず、最初のコマンドで失敗する** | 現在の導入状態で分岐する表に変更。**初回移行は `install.sh` / `install.ps1` → `aidlc config` から**、`aidlc update` はネイティブ導入済みの場合のみ |
| 🟡 | 追従チェックを `aidlc update --check` に置き換えたが、**比較対象は公開済みリリースであり、上流 `main` の追跡には使えない** | **現に `main` は 2.8.1 でリリースは 2.8.0** —— この流れに頼ると「最新です」と言われながら**本ノートが追跡している未リリースのコミットを丸ごと取りこぼす**。一次チェックは `main` の `AIDLC_VERSION` と CHANGELOG に戻し、`update --check` はリリース追従専用と位置づけた。両リポジトリの 4 箇所を同期 |
| 🟡 | 6.2 の「bun が要るのはソース生成時だけ」が、**同じ章の 6.5（`codekb-scope-diff` にネイティブ経路が無い）と矛盾** | bun が要る場合を 2 つに整理。**CodeKB の鮮度チェックを使うなら bun が要る**と明記 |

**3 件とも、同じ文書の別の箇所に反証が書いてあった。**
とくに 2 件目は、**自分が同じ PR で「main は 2.8.1、リリースは 2.8.0」と繰り返し書いていた**のに、
その事実を使う手順を書くときに参照しなかった。
**「この手順は、自分が書いた他の事実と両立するか」を書いた直後に確認する。**

#### 再レビュー 第 12 ラウンド（同日・Codex）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | 追従実績表が「`core/tools/*.ts` 51 → 69（+18。**他は全項目不変**）」としていたが、**`import.meta.main` を持つ起動可能な CLI も 32 → 39 に増えている**。内部 README も「CLI は 32 本」を現在値として提示したまま | 「**ワークフロー体系の指標は全項目不変**」に限定し、CLI 32 → 39 / ライブラリ 19 → 30 を明記。内部 README の 2 箇所には**当時の値である**旨と現在値を併記 |

**「他は全項目不変」は、自分の表の 2 行目を無視していた。**
同じレポートの §0.A に `うち import.meta.main 保持 | 32 | 39 | +7` と書いてあった。
**「不変」と書くときは、直前の表を上から順に指でなぞって確認する。**

#### 再レビュー 第 13 ラウンド（同日・Devin）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | 追従の定例チェックを**版定数と CHANGELOG だけ**にしていた。**どちらも変えないコミットは検出できない** | **一次チェックを「記録済み HEAD SHA と `origin/main` の比較」に変更**（`git ls-remote origin main` → 差があれば `git log --oneline <記録済みSHA>..origin/main`）。版・タグ確認は補助に降格。両リポジトリを同期 |
| 🟡 | bun が要る場面を「次の **2 つ**」と限定したが、**同じ章の Codex trust 生成手順（`bun scripts/package.ts codex trust`）が第 3 の経路**として存在する | 「本章に出てくる範囲では次の **3 つ**」に修正し、TUI の "Trust all" を使えば不要である旨も併記 |

**2 件目は「網羅を主張する一覧を、章を読み返さずに書いた」型。**
[[one-fact-many-copies-sync-them-mechanically]] に
「網羅性が要る一覧は、書きながら集めない」と記録した類型がまた出た。
**「次の N つ」と書くなら、その N を章全体の grep で確かめる。**

**1 件目は、自分が同期地点として HEAD SHA を記録しているのに、その SHA を使う手順を書かなかった。**
版番号は「変わったこと」を教えてくれるが、「変わっていないこと」は保証しない。

#### 再レビュー 第 14 ラウンド（同日・Devin）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | **前ラウンドで追加した追従手順自体が壊れていた。** `git ls-remote origin main` は SHA を表示するだけで `refs/remotes/origin/main` を更新しないため、直後の `git log` / `git diff` が**古い参照を見て「差分なし」と誤判定する** | **実測で確認**（`ls-remote` の前後で `git rev-parse origin/main` が変わらない）。`git fetch origin main --tags` を先に置く形へ修正。両リポジトリを同期 |
| 🔍 | 内部 `REMAINING_TASKS` 内で一次チェックの定義が 2 箇所で食い違っていた | 両方を SHA 比較に統一し、**「片方だけ直さないこと」**と相互参照を明記 |

**この検証の副産物として、上流がさらに進んでいたことが判明した。**
`git fetch` した結果、`c03f9e28` → **`52da70ad`**（3 コミット / 179 ファイル）。
**しかも 2.8.1 の CHANGELOG が書き換えられ、第 3 の修正が加わっていた** ——
**リリース済みの v2.8.0 では GitHub Copilot と Cursor のフックが動作しない**
（Copilot はクラッシュ、**Cursor は全ツール呼び出しがブロックされる**）。

**本ノートは `install.sh`（= `releases/latest` = v2.8.0）を推奨しているため、
この 2 ハーネスの読者に実害が出る。** 公開・内部の両方に警告を追加した。

**「自分の手順が壊れている」という指摘を検証したことで、より重い事実が見つかった。**
指摘の検証は、指摘そのものより広い範囲を見ることがある。

#### 再レビュー 第 15 ラウンド（同日・Devin）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🔍 | v2.8.0 の Copilot / Cursor 不具合を「**実測**」と書いたが、**§10 は実機再現も上流テストも未実施と明記している** | **根拠は上流コミット `52da70ad` の本文と CHANGELOG の読解であり、実機再現ではない。** 表記を訂正し、**両リポジトリの警告 5 箇所すべてと限界節に「実機再現していない」を明記**。導入判断に使う場合は自環境での確認を前提とする旨も追加 |

**重い事実を見つけた直後ほど、証拠の強さを言い過ぎる。**
不具合の内容は上流の一次情報として確かだが、**それは「上流がそう書いている」であって
「自分が動かして確かめた」ではない。**
このセッションでは「実測」を**コマンドを実行して確かめた事実**に限って使ってきたのに、
ここだけ読解に対して使ってしまった。
**証拠の種類（実行した / 読んだ / 推論した）をラベルで区別し続ける。**

#### 再レビュー 第 16 ラウンド（同日・Codex）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | Cursor の復旧手順が「**2.5.69 以降**へ更新」のままで、**同じ失敗様式が再発している v2.8.0 を「修正済みの範囲」に含めていた** | 症状に**原因が 2 つある**ことを明示し、切り分け表に変更。(a) 2.5.63〜2.5.68 → **2.5.69 以上 2.7.x 以下**へ。(b) v2.8.0 の再発 → **更新では直らない。修正は未リリース**。あわせて**Copilot のクラッシュ症状の行も追加**（同じ原因なのに表に無かった） |

**「修正済みの版以降なら安全」は、再発があると成り立たない。**
[[measured-at-revision-is-not-introducing-version]] に記録した
**「影響範囲の『以前』に修正版を含めてしまう」の裏返し**である ——
今回は**「以降」に再発版を含めてしまった**。
**範囲を書いたら、その範囲の中に「また壊れた版」が無いかを確認する。**

指摘は Cursor だけだったが、**同じ原因の Copilot 側がトラブルシュート表に無かった**ことに気づいた。
**指摘を直すときは、同じ原因の兄弟ケースを探す。**

#### 再レビュー 第 17 ラウンド（同日・Codex）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | **2.8.1 を「2 件の修正」と書いた箇所が、直前に自分で追記した「3 件目が加わった」と矛盾**していた | 上流は `AIDLC_VERSION` を 2.8.1 のまま CHANGELOG を書き換え、**Copilot / Cursor のフック修正を 3 件目として追加**していた。**「基準 `c03f9e28` 時点で 2 件」と範囲を明示**し、その後 3 件になった旨を併記。両リポジトリの 6 箇所を同期 |

**未リリース版の内容は、タグが付くまで増える。**
「2.8.1 は 2 件」は測定時点では正しかったが、**版番号が同じまま中身が増えた**。
**未リリース版について件数を書くときは、必ず基準コミットを添える。**

#### 再レビュー 第 18 ラウンド（同日・Devin）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | 前ラウンドで「2.8.1 の修正は 3 件」に直したが、**直後の検証手順は `c03f9e28` を checkout せよのまま**。**3 件目は `c03f9e28` より後のコミットなので、その SHA では検証できない** | **修正ごとに checkout する SHA が違う**ことを明示（Enter 受理 / umask は `c03f9e28` 以降、**Copilot / Cursor のフック修正は `52da70ad` 以降**）。両リポジトリ 3 箇所 |

**件数を直して、その件数を使う手順を直さなかった。**
前ラウンドの修正が、次のラウンドの指摘を生んだ形である。
**数を更新したら、その数を前提にしている手順・SHA・リンクを同じコミットで確認する。**

#### 再レビュー 第 19 ラウンド（同日・Codex）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | クイックスタートが `curl \| sh` の直後に `aidlc config` を呼んでいた。**インストーラは子シェルで走るため親シェルの PATH は更新されず、`aidlc: command not found` になる** | 上流 README も「If your shell cannot find `aidlc`, follow the PATH instruction printed by the installer or start a new shell」と書いている。**PATH を通す手順と `aidlc version` での確認をステップとして挿入**（README・6 章の両方） |

**手順を写すとき、上流の散文に書かれた前提を落とした。**
上流はコマンド列の**外**（散文）で PATH の注意を書いており、
コマンドだけを追うと落ちる。**手順を引き写すときは、その前後の散文も読む。**
→ [[read-upstream-prose-before-inferring]] と同じ型。

**自己点検の走査（今回導入）ではこれは検出できなかった。**
禁止パターンは「書いてはいけないこと」を捕まえるが、
**「書き落としたこと」は捕まえられない。** レビューが要る種類が残る。

#### 再レビュー 第 20 ラウンド（同日・Codex）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | 内部 README の版固定案内（`v2.8.0` か `c03f9e28`）が、**Copilot / Cursor ではどちらも壊れている**ことを示していなかった | **用途別の 3 行表**に変更。導入固定 → `v2.8.0`、評価ソースの照合 → `c03f9e28`、**Copilot / Cursor の不具合回避 → `52da70ad` 以降**と明記 |
| 🟡 | 内部 comparison の「AI-DLC には次のいずれも存在しない」3 項目が**旧のまま列挙**され、追記が「マイグレーション実行機構が部分的に解消」と**誤って帰属**していた | **3 項目を初版 / 2.7.2 以降の対照表に置き換え。** 版比較と in-place upgrade は解消、**ワークフロー記録の移行は依然として無い**と正確に分けた |

**2 件目は「追記で訂正する」やり方の限界だった。**
本文の 3 項目を残したまま下に注記を足したため、**注記がどの項目に掛かるかが曖昧**になり、
解消した 2 項目が「存在しない」と読める状態が続いていた。
**項目が個別に変化したときは、注記ではなく本文の項目そのものを表に組み替える。**

#### 再レビュー 第 21 ラウンド（同日・Codex）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | 「ネイティブバイナリの導入は必須」を**無条件に書いていた**が、**掛かるのは `runtime/` と `dist-release/` だけ**。チェックアウトから生成する **`dist/<harness>/` は Bun 前提でバイナリを呼ばない** | 前提の適用範囲を明記。**これにより Copilot / Cursor の回避策が成立することも示せた** —— `52da70ad` 以降を checkout して `dist/<harness>/` を生成すれば、**対応するバイナリが無くても動く**（bun が要る）。回避策の記述を具体化 |
| 🟡 | `08-v1-vs-v2.md` の「bun 制約は消えた」が、6.2 に列挙した 3 つの例外と矛盾 | 「通常の導入では消えた。ただし bun が要る場面は残る」に限定し 6.2 へリンク |
| 🟡 | `docs/DEVELOPMENT_RULES.md` の「版を固定するときは実在するタグを使う」が、**未リリース版の調査で古いツリーを選ばせる** | **タグか SHA のどちらかで固定**する形に改め、用途で使い分ける規則にした（リリース資産が要る手順はタグ、ソース照合は SHA。**どちらで固定したかを明記する**） |

**1 件目は、前ラウンドの修正が生んだ過剰一般化だった。**
「バイナリは代替ではなく前提」という訂正は正しかったが、**それを全経路に広げてしまい、
自分が数行下に書いた「`dist/` は Bun を呼ぶ」と矛盾した。**
**訂正を入れるときは、その訂正の適用範囲を同時に書く。**

**3 件目は自分で作った規則が実務を妨げていた例。**
「存在しないタグを指すな」という教訓から規則を作ったが、
**未リリース版を調査するという本ノート自身の作業を禁じる規則**になっていた。
**教訓から規則を作ったら、その規則を自分の現在の作業に当てて検算する。**

#### 再レビュー 第 22 ラウンド（同日・Devin）

**前ラウンドで書いた回避策そのものに 3 件。**

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | 生成コマンドの前に **`bun install --frozen-lockfile` が無い**。新規チェックアウトでは生成できない | 手順に追加 |
| 🟡 | 生成しただけで、**対象プロジェクトへ入れる操作が無い** | `bun dist/cursor/install.ts <project>` を追加し、完結した手順ブロックにした |
| 🔍 | 生成物は未検証と別の箇所で記録しているのに、ここでは「**動く**」と断定していた | 「**投影の呼び出し形からの読解であり、実機で確かめていない**」に緩和。**生成・導入・フック起動のいずれも未検証**と明記 |

**回避策を「示せた」と思った瞬間に、手順として成立するか確かめていなかった。**
`bun install` の欠落も、プロジェクトへ入れる操作の欠落も、
**自分が同じ章の別の節に正しい手順を書いている**のに参照しなかった。

**そして 3 件目が本質的である** —— 上流非推奨の経路を回避策として提示するなら、
**「動く」ではなく「試していない」と書くのが誠実**である。
前ラウンドで「バイナリ不要」を示せたことに満足して、**証拠の強さをまた言い過ぎた**
（第 15 ラウンドと同じ型）。

#### 再レビュー 第 23 ラウンド（同日・Devin）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | 回避手順が「Copilot なら `copilot`」と生成まで案内しながら、**導入操作は Cursor 用しか書いていなかった**。Copilot は同梱インストーラを持たずコピー方式なので、生成後に進めない | **ハーネス別に導入操作を分けた。** Cursor は `bun dist/cursor/install.ts`、**Copilot は `.aidlc/` / `aidlc/` / `.github/` / `AGENTS.md` のコピー**。**コピー後に folder trust が要る**ことも明記 |

**「または copilot」と書いた時点で、Copilot の導入方式が違うことを確認していなかった。**
自分が 6 章で「Copilot は他ハーネスと違い folder trust が要る」「インストーラではなくコピー」と
**繰り返し書いている**のに、回避策では Cursor の手順に「または」を足しただけで済ませた。
**「A または B」と書いたら、B についても手順を通しで書けるか確かめる。**

#### 再レビュー 第 24 ラウンド（同日・Devin）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🔴 | **`cp <checkout>/dist/copilot/AGENTS.md AGENTS.md` に「既存があればマージ」とコメントを付けていたが、`cp` は上書きであってマージではない。** 既存の `AGENTS.md` があるプロジェクトでは**利用者が書いた指示が失われる** | **退避 → `diff` → 手でマージ**の分岐に書き直した。既存が無い場合だけ `cp` する形にし、両リポジトリを同期 |

**このセッションで唯一の 🔴 であり、データ損失を招く手順だった。**
しかも**元の 6 章にあった同じコメント（「既存があればマージ」）を、疑わずに引き写した**結果である。
6 章の原文は `cp -R dist/copilot/... `の並びの最後に置かれていて、
**コマンドとコメントが食い違っていることに、書いた当時も今回も気づかなかった。**

**引き写したコマンドは、コメントが実際の動作と一致するか確かめる。**
とくに **`cp` に「マージ」と書いてあったら、それは誤りである。**

#### 再レビュー 第 25 ラウンド（同日・Devin）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🟡 | Copilot のコピー手順が **`.gitignore` の適用を落としていた**。省くと**利用者固有の状態がリポジトリに入る** | folder trust と `.gitignore` 適用を**コピー後の必須 2 ステップ**として明示 |
| 🔍 | 内部側のリンクが `[6.3 の GitHub Copilot 節](公開ノート 6.3 の GitHub Copilot 節)` という**リンク先が日本語の説明文になった壊れた形**だった | 平文の参照に修正。**内部リポジトリにもリンクチェッカーを掛けた**（残る 6 件はすべて `evidence/` の既存分） |

**2 件目は、公開側のリンクを内部側へ機械置換したときに壊した。**
`sed` で URL 部分ごと日本語に置き換えてしまい、**リンク構文としては成立するが飛べない**形になった。
公開側だけリンクチェッカーを回していたため検出できなかった。
**以後、内部リポジトリにもチェッカーを掛ける。**

**1 件目は、6 章に自分で書いた「コピー後の 3 ステップ」から 1 つだけ落とした。**
folder trust は引き写したが、`.gitignore` は落ちた。
**手順を引き写すときは、番号付きリストの項目数を数える。**

