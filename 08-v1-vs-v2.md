# 08. AI-DLC 1.x 系と Workflows 2.0 の差分

> **重要（一次確認の留保）**: 下表の「1.x 系」列は、AWS ブログ（Q Developer / Kiro 向けの初期 OSS 導入）・コミュニティ解説・v2 の Spaces 章（「旧リリースは単一フラット dir が新規実行で上書き」）からの **整理用対比**である。  
> **1.x 全体の確定仕様表ではない。** 正確な境界・名称は、リリース履歴と当該時点の README を一次確認すること。
> **なお 2026-09-01 の再編で 1.x の置き場所は `main` から新設の `v1` ブランチへ移った**（`main` は 2.x の正本になった）。
> 1.x を確認するときは `v1` ブランチを見ること（→ [16.1](./16-release-impact-2700.md#161-いちばん大きい変更はコードではなくブランチ)）。Amazon Q Developer 自体は現在も有効なサービスであり、「1.x の遺物」ではない。

---

## 8.1 大枠の対比（整理用・要一次確認）

| 観点 | 旧来のルール配布中心の運用（初期 OSS 周辺） | Workflows **2.0**（`main` ブランチ・確認済み） |
|------|---------------------------------------------|-------------------------------------------|
| 実体 | ルール／ステアリング Markdown をプロジェクトへコピーする運用が中心（当時の導入記事） | **TypeScript 決定論エンジン + skills/agents/hooks** |
| 方法論フェーズ | 方法論原典は主に 3（Inception/Construction/Operations） | 実装として **5 フェーズ 33 ステージ**（2.6.1 で 32 → 33） |
| ハーネス | 当時の導入は Q Developer / Kiro 等が中心（他エディタ向けコピーの言及あり） | **1 core → 7 配布**（Claude/Kiro IDE/Kiro CLI/Codex/opencode/Cursor/GitHub Copilot）。2.5.60 で GitHub Copilot、**2.5.63 で Cursor** が加わった |
| 状態 | **旧リリースの一部**は単一フラット dir が新規実行で上書きされ得た（v2 Spaces 章） | **Space / multi-Intent** で並行保持 |
| 適応 | 適応ワークフローの概念はあるが薄い、とする二次解説が多い | **11 scope + depth + test + Composer**（2.6.18 で `classic` / `express` が加わり 9 → 11 → [05](./05-scopes-depth-test.md)） |
| v1 相当の進行 | それが唯一の進行だった（Ideation フェーズ自体が無い） | **2.6.18 以降 `--scope classic` で選択可**（26/33 ステージ、**Ideation 7 本を全スキップ**）。しかも**暗黙の既定スコープ**でもある |
| 検証 | プロンプト規律中心、とする二次解説が多い | **Sensors + Reviewers + Verification Gates + Audit** |
| 学習 | 限定的、とする二次解説が多い | **Learning Loop → memory へ永続** |
| 実行単位 | 方法論上の bolt 概念 | **Walking skeleton / Ladder / Swarm**（**Bolt は 2.6.86 で「実行単位」から「計画上のスプリント様スライス」へ再定義された** → [01.8.1](./01-overview.md#181-bolt-の定義は-2686-で上流が書き換えた)） |
| 自律 | 限定的 | Construction **autonomous mode**（失敗時は halt） |
| 拡張 | ルール追記が中心 | **Plugins**（stages/scopes/agents）+ select-plugins |
| ビルド | ほぼコピー配布 | `bun scripts/package.ts` + drift guard |
| 仕様書 | Method Paper + ブログ | + **Workflows 2.0 Specification PDF**（`assets/`） |

### `classic` スコープ — 上流が公式に用意した「v1 相当のライフサイクル」

2.6.18 で追加された `classic` は、本章の主題（v1 と v2 の違い）に直接関わる。上流の scope 定義ファイル `core/scopes/aidlc-classic.md` は、その狙いをこう書いている:

> `classic` … **reproduces the AI-DLC v1 experience: the lifecycle begins after Ideation**, then adapts through Inception, Construction, and Operation according to each stage's applicability.

つまり v2 の側から見た「v1 との差分は Ideation フェーズの有無である」という整理が、**上流自身によってスコープという形で製品化された**ことになる。`classic` は Ideation 1.1–1.7 の 7 本をすべて SKIP し、Inception 以降の 26 ステージを保持する。しかもこれは 2.6.18 以降の**暗黙の既定スコープ**でもある（→ [05](./05-scopes-depth-test.md) 5.2.1）。

**ただし「v2 が v1 に戻った」わけではない。** `classic` を選んでも、Inception 以降の**承認ゲート・監査・レビュアー（`review_cap: advisory`）・センサー・決定論ルーティングはそのまま効く**。省かれるのは Ideation フェーズの 7 ステージだけで、上表の他の行（実体・状態・検証・学習・実行単位・自律・拡張）はいずれも `classic` でも v2 のままである。test strategy も depth から Standard を継承するので、`workshop` のような教育向けの緩和は入らない。**「v1 相当の進行を選べるようになった」以上の意味に読まないこと。**

---

## 8.2 2.0 で「自律が実用になる」根拠（README の主張）

1. **検証可能性**: ゲート・センサー・レビュア・監査
2. **自己修正**: 学習ループと再レビュー
3. **決定論ルーティング**: LLM に順序を丸投げしない
4. **承認ゲートで人間が最終決定**: レビュアも最終拒否権を持たない（autonomous 例外は [01](./01-overview.md)）
5. **ハーネス中立の正本**: 方法論の分裂を防ぐ

---

## 8.3 2.x マイナー系列で特に重要な進化（CHANGELOG から）

| 版 | 要点 |
|----|------|
| **2.6.1** | **Application Design → Domain Design（2.6）に改称し、Contract Design（2.8・CONDITIONAL）を新設。Delivery Planning は 2.9 へ。ステージが 32 → 33 に。** 設計成果物名も刷新され、**永続 state のスキーマが v8 に**（進行中ワークフローは移行不可）。**ただし 2.6.1 のガードには素通り経路があり、上げるなら 2.6.2 まで**（→ [12 章](./12-release-impact-2602.md)） |
| **2.5.75** | Observability を NFR の第一級成果物に（`observability-requirements` / `observability-design`） |
| **2.5.71** | ステージ横断の要素レベル・トレーサビリティ。**`traceability` センサー追加でセンサーが 5 → 6 本** |
| **2.5.67** | Construction 設計ステージを Unit 単位の **wave** で依存安全に並列実行 |
| **2.5.64** | 合成ワークフローのルーティングを決定論化（`validate-grid.nearest_stock` に一本化） |
| **2.5.63** | **Cursor 第 7 ハーネス**（IDE と CLI が同一の `.cursor/` を共有。導入は同梱インストーラ） |
| **2.5.37** | 質問フェンス(` ```question `)を生の文字列としてエコーしない契約を全ハーネス横断で明文化 |
| **2.5.36** | マルチリポジトリ向け任意 manifest `repos.json` と `aidlc-workspace-sync` 追加（ディスク検出が引き続き実行時の正） |
| **2.5.35** | Reverse Engineering 再実行時の codekb スキャン範囲の記録・比較（`codekb-scope-diff`）。前回知識の無警告破棄を防止 |
| **2.5.34** | プラグイン `adds.scopes` の実マージ（従来は drop-log のみ）。ステージ番号は engine が所有し `number:` は相対ヒント |
| **2.5.33** | ステージルールを `load-steering` で `run-stage` 前に確定配信。レビュアのチェックリストをビルド時に agent 本体へ埋め込み |
| **2.5.32** | 発見不能なプラグインセンサーマニフェストを degraded drop として `--doctor` に表示 |
| **2.5.31** | センサーの stdout 先頭ノイズ読み飛ばし。実 FAIL 判定の握りつぶしを解消 |
| **2.5.30** | scope frontmatter の `freeform_default: true`（プラグイン専用インストールの既定 scope 解決） |
| **2.5.26** | Reverse Engineering の 9 成果物は space レベル codekb ストアに書かれる旨を是正 |
| **2.5.25** | ARS 演算の決定論化（`aidlc-graph.ts ars` ＋ `tools/data/ars-priors.json`） |
| **2.5.17** | Kiro CLI/IDE のシェル許可リストをフルストリングマッチへ是正 |
| **2.5.12** | Kiro IDE 1.x 向け hook アダプタが stdin コンテキストチャネルを読むように |
| **2.5.11** | Intent claim のソース根拠・assumption 明示 |
| **2.5.10** | Kiro IDE hooks v2 schema 共存 |
| **2.5.9** | Codex compaction 文脈復元（CLI ≥0.145） |
| **2.5.6** | Kiro モデル pin 撤廃（spawn 修復） |
| **2.5.5** | レビュア完了のエンジン強制 |
| **2.5.2** | doctor --export |
| **2.5.1** | Composer エントロピー推定 |
| **2.5.0** | ensemble: `mode` に pipeline / mob を追加、`subagent` を hub-and-spoke 形状に強化 |
| **2.4.6** | **opencode** 第 5 ハーネス |
| **2.4.5** | ネイティブ dispatcher / binaries / config・plugin コマンド族 |
| **2.4.0** | 敵対的レビュー契約 |
| **2.3.x** | プラグイン選択、センサー精度、状態 Phase Progress 等 |

上表は 2.5.37 以前を主対象に整理したうえで、**2.6.2 までの構造変化に関わる版だけを先頭に追記**したものである。
**2.5.38〜2.5.62 は上表に載せていない**（→ [11-release-impact-2562.md](./11-release-impact-2562.md) の 11.6）。
**2.5.63〜2.6.2 の全 12 エントリ**も同様に別章で扱う（→ [12-release-impact-2602.md](./12-release-impact-2602.md)）。
**2.6.3〜2.6.49 の 24 エントリ**も上表には載せていない（スコープ体系の変更は [05](./05-scopes-depth-test.md)、その他は個別章を参照）。

本章の 8.1〜8.2 / 8.4〜8.5 の整理は **2.5.37**（2026-08-03 時点 CHANGELOG）を土台に書かれている。
**上流はその後 2.7.0（CHANGELOG 日付 2026-09-01 / 取得日 2026-09-01）まで進んでいる**。8.1 の数値は **2.7.0 実測**（**5 フェーズ 33 ステージ・スコープ 11・ハーネス 7 種**）である。
フェーズ・ステージ・ハーネスの 3 つは 2.6.2 から不変で、**スコープだけが 2.6.18 で 9 → 11** に増えた。
2.6.49 → 2.6.55 では**中核メトリクスが全項目不変**で、`stage-graph.json` / `scope-grid.json` はバイト単位で一致する（→ [14-release-impact-2655.md](./14-release-impact-2655.md)）。
**2.6.55 → 2.6.123 でもこの 4 つは動いていないが、TypeScript フックが 17 → 18、
`core/tools/*.ts` が 41 → 51 になった**（→ [15-release-impact-26123.md](./15-release-impact-26123.md)）。
**2.6.123 → 2.7.0 では中核メトリクスが全項目不変**である（→ [16-release-impact-2700.md](./16-release-impact-2700.md)）。

監査イベントの**種別数**は 2.5.62 → 2.6.2 では **82 種（21 分類）のまま増減が無かった**が、
**2.6.49 で 86 種（22 分類）になった**。追加は `DOCUMENT_INDEXED` / `DOCUMENT_UPDATED` / `DOCUMENT_REMOVED` / `PIPELINE_LINK_COMPLETED` の 4 件で、**削除は 0 件**。分類が 21 → 22 になったのは `Documents`（3 イベント）が加わったためで、**その追加自体は区間内の 2.6.15**（`PIPELINE_LINK_COMPLETED` は 2.6.39）。**2.6.49 で新設された分類ではない。**
**その後 2.6.123 で 91 種になった**（分類は 22 のまま。→ [15.1](./15-release-impact-26123.md#151-数値の変化)）。**2.7.0 でも 91 種のままである。**
出典は正典レジストリ `core/knowledge/aidlc-shared/audit-format.md` の Event Registry 見出し。**`docs/reference/12-state-machine.md` は同じイベント集合を 19 分類と数えており、分類数は出典によって違う**（上流自身が "the grouping is presentational, the event set is the invariant" と注記）。
なお 2.5.62 → 2.6.2 のときと同様、**「種別が同数＝監査に変化なし」ではない**（記録される行の中身は別途変わる → 12 章 12.1）。
→ [11-release-impact-2562.md](./11-release-impact-2562.md)（2.5.37 → 2.5.62）／[12-release-impact-2602.md](./12-release-impact-2602.md)（2.5.62 → 2.6.2）

> 2.5.12〜2.5.37 の 12 リリースを俯瞰すると、**決定論の強化**（2.5.25 の ARS 演算、2.5.33 のルール配信、2.5.37 の質問レンダリング契約）と、**サイレント失敗の解消**（2.5.26 / 2.5.31 / 2.5.32 / 2.5.35）が大半を占める。機能追加よりも「チェックが黙って素通りする経路を塞ぐ」方向に開発資源が向けられている。

---

## 8.4 既存ルール資産との関係

- 初期 OSS 時代のルール／ステアリング配布は「方法論をエージェントに読ませる」第一世代、と位置づけられることが多い
- 2.0 は同じ方法論を **実行時エンジンとして再実装**しており、単純なルールコピー互換ではない
- チーム規約は 2.0 では `aidlc/spaces/.../memory` と `knowledge` に載せ替えるのが正攻法

---

## 8.5 いつ 2.0 を使うか

| 向く | 向かない／慎重 |
|------|----------------|
| 複数セッションにまたがる機能開発 | 1 ファイルの超短期パッチのみ（オーバーヘッド大） |
| 監査・トレースが必要 | モデルが弱くゲートを雑に突破する環境 |
| チームで method を共有したい | **bun** を入れられない／選んだハーネスの認証・モデル前提を満たせない（Bedrock は全ハーネス必須ではない → [06](./06-harnesses-install.md)） |
| ハーネス横断で同じ工程を回したい | 既存ルール資産の無変更移行だけが目的 |

---

> **上流 2.6.2 → 2.6.49 の差分**: [13-release-impact-2649.md](./13-release-impact-2649.md)
