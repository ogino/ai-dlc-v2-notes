# 公開コンテンツ・ポリシー

本リポジトリは **PUBLIC** である。コミットする前に本ポリシーを確認すること。

---

## 判断基準

追加しようとしている記述について、次を自問する。

> **上流 OSS（awslabs/aidlc-workflows）のリポジトリ URL を示せば、読者が同じ結論に到達できるか。**

- **Yes** → 本リポジトリに置いてよい
- **No** → 置いてはならない

判定は**章単位ではなく文単位**で行う。1 つの段落の中に、公知の技術的事実と、そうでない情報が混在しうるため。

---

## 置いてよいもの

- 上流 OSS のソースコード・ドキュメント・CHANGELOG から確認できる事実
- 上記に対する技術的な分析・解釈（`ファイル:行番号` で追跡可能なもの）
- 一般に公開されているブログ記事・仕様書の要約
- 業界一般論として成立し、特定の組織との紐付けが不可能な記述

## 置いてはならないもの

| 類型 | 例 |
|------|-----|
| **選定・商談の進行状況** | 「〜を評価中」「〜の選考において」。**匿名化しても、進行中の案件が存在する事実自体が機密** |
| 組織名・顧客名およびその識別子 | 社名、社名を含むディレクトリ名・エージェント名・スコープ名 |
| 特定組織の業務ドメイン | 特定の業界・技術領域と組織を結びつける記述 |
| 内部システム・内部リポジトリの名称 | 社外に公開されていないツール名・リポジトリ名 |
| 組織の開発標準の具体値 | カバレッジ目標、デプロイ戦略、denylist の内容、内製スキルの構成 |
| 事業判断 | リリース順序、採用・不採用の判断、FS の結論 |
| 個人を特定できる情報 | ホームディレクトリの絶対パス、業務用でないメールアドレス |
| 認証情報 | トークン、API キー、秘密鍵 |

---

## 匿名化について

**匿名化は技術的事実の一般化にのみ使う。組織名・商談情報には使わない。**

判断の目安は次のとおり。

> 匿名化後の文章から、悪意ある読者が元の情報を高い精度で復元できるか。
> 復元できるなら、匿名化ではなく**除外**する。

例えば組織名を「国内の通信事業者」と一般化しても、業界 × 規模 × 時期の組み合わせから特定されうる。この場合は記述ごと除外する。

---

## コミット前の手順

1. 追加・変更した差分を `git diff` で全件目視する
2. 本ポリシーの「置いてはならないもの」に照らす
3. CI のリークチェックが通ることを確認する（PR で自動実行される）

CI はあくまで最後の網であり、**一次的な責任は書く側にある**。CI が検出できるのは既知のパターンのみで、新しい類型は検出できない。

---

## リークチェックの仕組み

`.github/workflows/leak-check.yml` が PR と push で以下を実行する。

| チェック | 内容 |
|----------|------|
| gitleaks | トークン・API キー・秘密鍵の検出（汎用パターン） |
| 個人パス検出 | ホームディレクトリの絶対パスの検出 |
| 禁止語スキャン（内容） | ファイルの中身と禁止語リストの照合 |
| 禁止語スキャン（パス） | ディレクトリ名・ファイル名と禁止語リストの照合。パス自体に禁止語が現れるケースは内容スキャンでは捕捉できないため別途行う |

### 禁止語リストを平文で置かない理由

禁止語リストには、守るべき組織名・内部名称そのものを書く必要がある。**それを公開リポジトリにコミットすれば、守ろうとしている情報を平文で公開することになる。**

そのため本リポジトリでは、リストを **GitHub Actions Secrets**（`DENYLIST_PATTERNS`）に格納し、CI 実行時のみ展開する。リポジトリ内にはリストの実体を置かない。

### 禁止語リストの設定・更新

リポジトリ管理者が次のコマンドで設定する（1 行 1 パターン、`grep -E` の正規表現）。

```bash
gh secret set DENYLIST_PATTERNS --repo <owner>/<repo> < patterns.txt
rm patterns.txt   # ローカルに残さない
```

新しい機密の類型が判明したら、その都度リストを更新する。

### fail-closed の方針

**`DENYLIST_PATTERNS` が未設定の場合、CI は成功ではなく失敗する。** 未設定を警告のまま通すと、検査が実行されていないのに緑になり、「チェックが通った」という誤った安心を与えるため。

**fork からの pull_request も例外としない。** GitHub の仕様上 secrets が渡らず構造的に実行不能だが、警告で通すと外部 PR が禁止語を追加しても緑になりマージできてしまう。そのため **意図的に失敗させる**。fork PR はメンテナが本ポリシーに照らして手動で確認し、問題なければ管理者権限でマージする。

同じ理由で、禁止語スキャン中に `grep` が終了コード 2 以上（正規表現の構文エラー等）を返した場合も、検出なしとはせず job を失敗させる。
